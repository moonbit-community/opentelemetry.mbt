# Resource Semantic Conventions

This package re-exports resource-oriented semantic-convention constants, such as
resource attributes describing service identity, cloud environment, container
metadata, runtime details, and deployment metadata.

Use these constants when populating `sdk/resource.Resource` or when setting
resource attributes directly on provider builders.
