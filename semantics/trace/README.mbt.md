# Trace Semantic Conventions

This package re-exports trace-oriented semantic-convention constants, such as
HTTP, database, messaging, RPC, exception, and client/server attribute keys.

Use these constants when setting span attributes or when naming trace-related
measurements consistently with the OpenTelemetry semantic-convention schema.
