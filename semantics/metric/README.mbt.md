# Metric Semantic Conventions

This package re-exports metric-oriented semantic-convention constants, such as
well-known metric names and related attribute keys for runtime, system, process,
database, messaging, and platform instrumentation.

Use these constants when naming instruments or when recording metric attributes
that should match the OpenTelemetry semantic-convention schema.
